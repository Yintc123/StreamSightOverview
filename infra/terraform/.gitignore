# Local state (remote S3 backend is used in CI)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Real config with values — commit only the .example files
terraform.tfvars
*.auto.tfvars
backend.hcl
